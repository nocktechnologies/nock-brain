# Nock-Brain Session Handoff — 2026-08-09

**From:** Fable (agent `fable-nock-brain`, NockCC bus)
**Counterpart:** Mira (`mira-nockos`) — she gates every PR; live seat is `mira-residentd` on nock-fleet-02.
**Build split (Kevin's ruling):** Fable builds in base `nock-brain`; Mira gates + operates on fleet-02. Merges need Mira's gate OR Kevin's explicit word. Fleet SSH: `ssh nock-fleet-02` (passwordless sudo; `~/.nock-brain` is the live store, owned by `nock`).

## TL;DR
The audit's core finding — "the brain engine works but is unplugged from Mira's mouth, one real integrity hole, and a lot not turned on" — is **fixed**. Recall now fires on both her surfaces, the integrity hole (silent resurrection) is sealed and enforced in production, and the whole green-lit backlog is **merged**. Every legitimate finding across ~2 dozen (CodeRabbit ×N, an adversarial nock-review, Mira's gate incl. a post-merge catch) was closed with a proof test. Nothing bad reached her 2,111-fact live store.

`main` HEAD at handoff: `dc1f343`. Full suite ~604 tests green.

## What shipped (all MERGED)
- **S1** signed revocation events (#64) + `backfill-revocations` (#65, **run live**: 46 attested, 0 resurrected/invalid/unattested, facts.json byte-identical) + `_revoke` single-exit-invariant consolidation (#66). Closes silent-resurrection: a superseded fact flipped back to `current` fails `verify-facts` (exit 4). Keyring model handles rotation via `--retired-pub`.
- **F1** nock-brain recall wired into BOTH of Mira's surfaces: daemon composer (#36 — verify state below) and resident `capsule_hook` (#8, merged — identity injection proven byte-identical whether recall off/errors). S2 dedupe rides both.
- **F2** contradiction nightly made finishable (`detect-contradictions --llm-top`, default 25) + `nockbrain-health --expect-contradictions` stale-queue flag (#67). **Live**: schedule 41 now runs `--llm-top 25`.
- **F3** insight shape-gate at generation (`is_valid_lesson`, kills the "Once you share those…" pollution incl. quote-wrap/curly-apostrophe bypasses) + `synthesize --sign` (#68).
- **S3+S8** nonce-bound watermarked job windows `bin/_window.py` (#70) — module only; NOT yet wired into the nightly commands.
- **S6** per-kind decay was already in base (better than mnemosyne's). **S7** source-trust recall discount (`trust:untrusted` / `provenance:external_write|mcp|tool` → 0.5× until promoted) (#69). Ready for E7's MCP write path.
- **S4** projection readback receipts `bin/_projection.py` (write→readback→sha256→applied|ambiguous, never raises) + `export-graph`/`export-obsidian --receipt` + `health --projection-receipts` (#73).
- **S9** `bin/edit-fact.py` (#72, merged on Kevin's word) + short-write/ftruncate durability (#74, #75). The write primitive: unique-match replace, actor history, crash-durable ordering, retryable+self-healing revert, out-of-band + double-revert + parent-cascade + v2-claim refusal, sibling-loss guard, 0600 sidecar, flock, all-or-nothing append. Survived CodeRabbit ×2 + adversarial nock-review (10 findings, incl. a HIGH that would have TAMPERED a v2-claim fact).

## Open / owed (NOT Fable's — do not "build ahead")
1. **#36 (nock-agent-harness daemon composer)** — verified-clean both sides, but was merge-blocked on CodeRabbit rate-limit at its head. VERIFY current state before assuming merged. Inactive daemon; nothing live depends on it.
2. **Mira operate-side (hers):** backup retention policy (14 daily + 8 weekly, E1); wire the new nightly flags — `--llm-top`/`--expect-contradictions` (done for 41?), `synthesize --sign`, `--projection-receipts` in the health run; apply the **S10 residence routing files** she already pushed (root `INDEX.md` locations table + `CLAUDE.md` read-order realign + `BRAIN.md:68` pin-drift fix) — these structurally cure her pin-amnesia loop.
3. **E2 SQLite cutover — PARKED behind Kevin's gate.** Code merged earlier (store contract, `SqliteStore`, fail-closed migrator, parity harness). Design: `docs/specs/2026-07-28-e2-sqlite-store-design.md`. Migration needs ZERO re-sign (attestations cover values, proven). Cutover = flag `NOCKBRAIN_STORE=sqlite` after a 7-nightly parity soak. **Fold S1's `revocations.jsonl` into the SQLite schema at cutover, and pin schema+key_id into the signed payload** (already done for revocations; check insights).

## Watch-outs for next session
- **Mira gates hard and is usually right** — every hold this run was a real bug, including one found *post-merge*. Turn her findings around fast with a proof test; do not waive on the write primitive.
- **CodeRabbit "rate limited" ≠ reviewed** — it can falsely claim "already reviewed" a parent commit; force `@coderabbitai full review` at the actual head (Mira caught this on #74).
- **Two review tools:** CodeRabbit (auto on PRs) and an adversarial nock-review subagent (spawn a general-purpose/Explore agent to probe the real on-disk file). Use both on anything that mutates the live store.
- **NockCC bus:** send via REST `POST https://cc.nocktechnologies.io/api/teams/messages/` with `--data-binary @file` (curl `-d` mangles JSON) + `X-API-Key` from `~/Dev/claude-remote-manager/agents/mira/.env.local`. Read a thread: `GET /api/teams/messages/conversations/fable-nock-brain::mira-nockos/?agent_name=fable-nock-brain`. `nockcc_agent_send_message`/`_reply` MCP tools are OAuth-read-only for this seat — use REST for writes.
- **Docs:** `docs/specs/2026-07-26-mira-brain-enhancement-plan.md` (v2, the whole roadmap + decision log) and the E2 spec are the source of truth.

## Roadmap items NOT built (were never green-lit or are post-cutover)
- E3 reranker (embedder eval-gated), E4 taxonomy/decay tiers beyond S6, E7 MCP pull path/write-lane (S7 is its trust half). Sequence per the enhancement-plan v2 after E2.
