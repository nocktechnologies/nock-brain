# AGENTS.md — nock-brain

The repo map is `CLAUDE.md` at this root. Read it first: it names the
non-negotiables and points at `docs/REPO-MAP.md`, which maps every module, the
end-to-end pipeline, the recall order, the integrity model, and the sharp edges.
Read the map before exploring code, then the `docs/` entry (§10) for your area.
A PR that changes a module's contract updates `docs/REPO-MAP.md` in the same PR.
